openapi: 3.0.3
info:
  title: Merge Conflict Path
  version: 1.0.0
paths:
  /items:
    post:
      operationId: createItem
      responses:
        '201':
          description: Created
components:
  schemas:
    Other:
      type: object
      properties:
        name: { type: string }
