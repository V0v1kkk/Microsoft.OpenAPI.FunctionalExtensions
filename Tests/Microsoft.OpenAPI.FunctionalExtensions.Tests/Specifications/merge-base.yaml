openapi: 3.0.3
info:
  title: Merge Base
  version: 1.0.0
paths:
  /items:
    get:
      operationId: listItems
      responses:
        '200':
          description: OK
components:
  schemas:
    Item:
      type: object
      properties:
        id: { type: string }
